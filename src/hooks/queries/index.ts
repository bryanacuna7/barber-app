/**
 * Query Hooks - Central Export
 *
 * Custom React Query hooks for all 7 UI/UX redesign modules.
 *
 * Created: Session 110 (Pre-Implementation Requirements)
 */

// Appointments hooks (Mi Día)
export * from './useAppointments'

// Services hooks (Servicios)
export * from './useServices'

// Clients hooks (Clientes)
export * from './useClients'

// Barbers hooks (Barberos)
export * from './useBarbers'

// Analytics hooks (Reportes)
export * from './useAnalytics'

// Calendar hooks (Citas)
export * from './useCalendar'

// Settings hooks (Configuración)
export * from './useSettings'

// Barber Blocks hooks (Bloqueos / Vacaciones)
export * from './useBarberBlocks'
